// The badge moved into the primitives so the board, the market page and the
// sibling strip share one. Re-exported here so nothing has to move twice.
export { PhaseBadge as default } from "@/components/ui/primitives";
